<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="real_estate_action" model="ir.actions.act_window">
        <field name="name">real.estate.action</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_avilable':True}</field>
    </record>

    <record id="real_estate_view_list" model="ir.ui.view">
        <field name="name">real.estate.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Real Estate List" decoration-muted="state == 'sold'" decoration-bf="state == 'offer_accepted'" decoration-success="state in ('offer_received','offer_accepted')">
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="True" />
            </list>
        </field>
    </record>

    <record id="real_estate_view_form" model="ir.ui.view">
        <field name="name">real.estate.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_sold" type="object" string="SOLD" invisible="state in ('offer_received`','new','sold','canceled')" />
                    <button name="action_cancel" type="object" string="CANCEL" invisible="state in ('sold','canceled')"/>
                    <field name="state" widget="statusbar" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <div class="oe_title">
                        <field name="tags_ids" widget="many2many_tags" options="{'color_field': 'color'}" ></field>
                    </div>
                    <br/>
                    <group>
                        <group>
                            <!-- <field name="state" string="status" optional="hide" /> -->
                            <field name="estate_property_type_id" options="{'no_create': True, 'no_edit': True}"></field>
                            <field name="postcode"></field>
                            <field name="date_availability"></field>
                        </group>
                        <group>
                            <field name="expected_price"></field>
                            <field name="best_price" />
                            <field name="selling_price"></field>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group >
                                <field name="description"></field>
                                <field name="bedrooms"></field>
                                <field name="living_area"></field>
                                <field name="facades"></field>
                                <field name="garage"></field>
                                <field name="garden"></field>
                                <field name="garden_area" invisible="not garden"></field>
                                <field name="garden_orientation" invisible="not garden"></field>
                                <field name="total_area"></field>
                            </group>
                        </page>
                        <page string="offers">
                            <field name="offer_ids"></field>
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="buyer_id"></field>
                                <field name="salesperson_id"></field>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="real_estate_view_search" model="ir.ui.view">
        <field name="name">real.estate.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"></field>
                <field name="postcode"></field>
                <field name="expected_price"></field>
                <field name="bedrooms"></field>
                <field name="living_area" filter_domain="[('living_area','>=', self)]"></field>
                <field name="facades"></field>
                <filter string="avilable" name="avilable" domain="['|',('state','=','new'),('state','=','offer_received')]"></filter>
                <filter name="Postcode" string="Postcode" context="{'group_by': 'postcode'}" />
            </search>
        </field>
    </record>

</odoo>